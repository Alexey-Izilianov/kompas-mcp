// Ресурсы библиотеки «Давинчи»: меню подключённой библиотеки (id = LIBRARYID)
// и строка имени библиотеки. IDR_API7 = маркер API-7-библиотеки (SDK).
// Файл в UTF-8: без pragma rc.exe прочитает кириллицу в системной cp1251 — мусор.
#pragma code_page(65001)
100 MENU DISCARDABLE
BEGIN
  MENUITEM "Панель", 1
  MENUITEM "Диагностика", 2
END

STRINGTABLE DISCARDABLE
BEGIN
  100 "Давинчи"
  102 "kAPI7.dll"
END